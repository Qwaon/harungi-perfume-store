# UX Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Точечная полировка UI — hover на карточках, page transitions, stagger-анимации, dot-индикаторы в галерее, SVG-иконки в TrustStrip.

**Architecture:** Все изменения изолированы по файлу — никакой общей логики между задачами. PageTransition — новый компонент, подключается в layout. Остальное — правки существующих компонентов.

**Tech Stack:** Next.js 14 App Router, Framer Motion, Tailwind CSS, TypeScript

---

## File Map

| Файл | Действие |
|---|---|
| `src/components/ProductCard.tsx` | Modify — hover translateY + brand color |
| `src/components/PageTransition.tsx` | Create — новый компонент |
| `src/app/layout.tsx` | Modify — обернуть children |
| `src/components/HowToOrder.tsx` | Modify — stagger на цифрах |
| `src/components/ImageGallery.tsx` | Modify — rounded стрелки + dot-индикаторы |
| `src/components/TrustStrip.tsx` | Modify — SVG-иконки |

---

### Task 1: ProductCard — hover lift + brand color

**Files:**
- Modify: `src/components/ProductCard.tsx`

- [ ] **Step 1: Добавить hover-эффект подъёма на карточку и transition бренд-лейбла**

Заменить текущий `<div className="group">` блок целиком:

```tsx
// src/components/ProductCard.tsx
// Найти строку:
      <div className="group">
// Заменить на:
      <div className="group transition-transform duration-300 ease-out hover:-translate-y-1">
```

Найти строку с брендом:
```tsx
// Найти:
          <p className="label text-ink-300 mb-1">{perfume.brand}</p>
// Заменить на:
          <p className="label text-ink-300 group-hover:text-gold-500 transition-colors duration-300 mb-1">{perfume.brand}</p>
```

- [ ] **Step 2: Проверить сборку**

```bash
npm run build
```

Ожидаемый результат: `✓ Compiled successfully`, без ошибок TypeScript.

- [ ] **Step 3: Commit**

```bash
git add src/components/ProductCard.tsx
git commit -m "feat: card hover lift and brand label color transition"
```

---

### Task 2: PageTransition — fade при смене страниц

**Files:**
- Create: `src/components/PageTransition.tsx`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Создать компонент PageTransition**

```tsx
// src/components/PageTransition.tsx
'use client';

import { motion, AnimatePresence } from 'framer-motion';
import { usePathname } from 'next/navigation';

export default function PageTransition({ children }: { children: React.ReactNode }) {
  const pathname = usePathname();

  return (
    <AnimatePresence mode="wait">
      <motion.div
        key={pathname}
        initial={{ opacity: 0, y: 8 }}
        animate={{ opacity: 1, y: 0 }}
        exit={{ opacity: 0, y: -4 }}
        transition={{ duration: 0.3, ease: [0.22, 1, 0.36, 1] }}
      >
        {children}
      </motion.div>
    </AnimatePresence>
  );
}
```

- [ ] **Step 2: Подключить PageTransition в layout.tsx**

Открыть `src/app/layout.tsx`. Добавить импорт после существующих импортов:

```tsx
import PageTransition from '@/components/PageTransition';
```

Найти строку:
```tsx
          <main className="flex-1 pb-20 md:pb-0">{children}</main>
```

Заменить на:
```tsx
          <main className="flex-1 pb-20 md:pb-0">
            <PageTransition>{children}</PageTransition>
          </main>
```

- [ ] **Step 3: Проверить сборку**

```bash
npm run build
```

Ожидаемый результат: `✓ Compiled successfully`, без ошибок TypeScript.

- [ ] **Step 4: Commit**

```bash
git add src/components/PageTransition.tsx src/app/layout.tsx
git commit -m "feat: add page transition fade animation"
```

---

### Task 3: HowToOrder — stagger-анимация на цифрах

**Files:**
- Modify: `src/components/HowToOrder.tsx`

- [ ] **Step 1: Добавить 'use client' и Framer Motion**

Текущий файл — серверный компонент без директивы. Нужно добавить клиентский режим и анимации.

Заменить весь файл целиком:

```tsx
// src/components/HowToOrder.tsx
'use client';

import Link from 'next/link';
import { motion } from 'framer-motion';

const STEPS = [
  {
    num: '01',
    title: 'Выберите аромат',
    desc: 'Откройте каталог, используйте фильтры по типу, полу или бренду.',
  },
  {
    num: '02',
    title: 'Добавьте в корзину',
    desc: 'Выберите объём — от 5 мл. Можно добавить несколько ароматов.',
  },
  {
    num: '03',
    title: 'Оставьте заявку',
    desc: 'Укажите имя и Telegram или телефон. Никакой предоплаты.',
  },
  {
    num: '04',
    title: 'Получите заказ',
    desc: 'Мы свяжемся для подтверждения. Доставка по России или самовывоз в Ставрополе.',
  },
];

export default function HowToOrder() {
  return (
    <section className="max-w-7xl mx-auto px-4 sm:px-6 py-16 md:py-24">
      <div className="flex items-baseline justify-between mb-10">
        <h2 className="section-title">Как заказать</h2>
        <Link href="/catalog" className="text-sm text-gold-500 hover:text-ink-900 transition-colors duration-200">
          В каталог →
        </Link>
      </div>
      <div className="grid grid-cols-2 md:grid-cols-4 gap-6 md:gap-8">
        {STEPS.map((step, i) => (
          <div key={step.num} className="flex flex-col gap-3">
            <motion.span
              className="font-display text-4xl font-light text-cream-300"
              initial={{ opacity: 0, y: 16 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.5, delay: i * 0.1, ease: [0.22, 1, 0.36, 1] }}
            >
              {step.num}
            </motion.span>
            <h3 className="font-medium text-ink-900 text-sm tracking-wide">{step.title}</h3>
            <p className="text-xs text-ink-500 leading-relaxed">{step.desc}</p>
          </div>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Проверить сборку**

```bash
npm run build
```

Ожидаемый результат: `✓ Compiled successfully`, без ошибок TypeScript.

- [ ] **Step 3: Commit**

```bash
git add src/components/HowToOrder.tsx
git commit -m "feat: stagger animation on HowToOrder step numbers"
```

---

### Task 4: ImageGallery — rounded стрелки + dot-индикаторы

**Files:**
- Modify: `src/components/ImageGallery.tsx`

- [ ] **Step 1: Округлить стрелки и добавить dot-индикаторы**

Заменить весь файл целиком:

```tsx
// src/components/ImageGallery.tsx
'use client';

import Image from 'next/image';
import { useState } from 'react';
import { motion, AnimatePresence } from 'framer-motion';

interface Props {
  images: string[];
  name: string;
}

export default function ImageGallery({ images, name }: Props) {
  const [active, setActive] = useState(0);
  const [touchStartX, setTouchStartX] = useState<number | null>(null);
  const [touchStartY, setTouchStartY] = useState<number | null>(null);

  const handleTouchStart = (e: React.TouchEvent) => {
    setTouchStartX(e.touches[0].clientX);
    setTouchStartY(e.touches[0].clientY);
  };

  const handleTouchEnd = (e: React.TouchEvent) => {
    if (touchStartX === null || touchStartY === null) return;
    const dx = touchStartX - e.changedTouches[0].clientX;
    const dy = touchStartY - e.changedTouches[0].clientY;
    if (Math.abs(dx) > Math.abs(dy) && Math.abs(dx) > 40) {
      if (dx > 0) setActive((prev) => (prev + 1) % images.length);
      else setActive((prev) => (prev - 1 + images.length) % images.length);
    }
    setTouchStartX(null);
    setTouchStartY(null);
  };

  return (
    <div className="flex flex-col gap-4">
      {/* Main image */}
      <div
        className="relative overflow-hidden bg-cream-200 aspect-square rounded-sm"
        onTouchStart={handleTouchStart}
        onTouchEnd={handleTouchEnd}
      >
        <AnimatePresence mode="wait">
          <motion.div
            key={active}
            className="absolute inset-0"
            initial={{ opacity: 0, scale: 1.04 }}
            animate={{ opacity: 1, scale: 1 }}
            exit={{ opacity: 0 }}
            transition={{ duration: 0.5, ease: [0.22, 1, 0.36, 1] }}
          >
            <Image
              src={images[active]}
              alt={name}
              fill
              sizes="(max-width: 1024px) 100vw, 50vw"
              className="object-cover"
              priority={active === 0}
            />
          </motion.div>
        </AnimatePresence>

        {/* Arrow nav */}
        {images.length > 1 && (
          <>
            <button
              onClick={() => setActive((prev) => (prev - 1 + images.length) % images.length)}
              className="absolute left-3 top-1/2 -translate-y-1/2 w-9 h-9 rounded-full bg-cream-50/80 backdrop-blur-sm flex items-center justify-center hover:bg-cream-50 transition-colors"
              aria-label="Предыдущее фото"
            >
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
                <path d="M10 3L5 8l5 5" stroke="#141413" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
            </button>
            <button
              onClick={() => setActive((prev) => (prev + 1) % images.length)}
              className="absolute right-3 top-1/2 -translate-y-1/2 w-9 h-9 rounded-full bg-cream-50/80 backdrop-blur-sm flex items-center justify-center hover:bg-cream-50 transition-colors"
              aria-label="Следующее фото"
            >
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
                <path d="M6 3l5 5-5 5" stroke="#141413" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
            </button>
          </>
        )}

        {/* Dot indicators */}
        {images.length > 1 && (
          <div className="absolute bottom-3 left-1/2 -translate-x-1/2 flex items-center gap-1.5">
            {images.map((_, i) => (
              <button
                key={i}
                onClick={() => setActive(i)}
                aria-label={`Фото ${i + 1}`}
                className={`rounded-full bg-ink-900/70 transition-all duration-300 ${
                  i === active ? 'w-4 h-1.5' : 'w-1.5 h-1.5 opacity-40'
                }`}
              />
            ))}
          </div>
        )}
      </div>

      {/* Thumbnails */}
      {images.length > 1 && (
        <div className="flex gap-3">
          {images.map((src, i) => (
            <button
              key={src}
              onClick={() => setActive(i)}
              className={`relative overflow-hidden aspect-square flex-1 rounded-sm transition-all duration-200 ${
                active === i
                  ? 'ring-1 ring-ink-900 ring-offset-2 ring-offset-cream-100'
                  : 'opacity-50 hover:opacity-80'
              }`}
            >
              <Image
                src={src}
                alt={`${name} ${i + 1}`}
                fill
                sizes="120px"
                className="object-cover"
              />
            </button>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Проверить сборку**

```bash
npm run build
```

Ожидаемый результат: `✓ Compiled successfully`, без ошибок TypeScript.

- [ ] **Step 3: Commit**

```bash
git add src/components/ImageGallery.tsx
git commit -m "feat: rounded gallery arrows and dot indicators"
```

---

### Task 5: TrustStrip — SVG-иконки

**Files:**
- Modify: `src/components/TrustStrip.tsx`

- [ ] **Step 1: Заменить dots на SVG-иконки**

Заменить весь файл целиком:

```tsx
// src/components/TrustStrip.tsx
const TRUST_ITEMS = [
  {
    label: 'Оригиналы с гарантией',
    icon: (
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" strokeLinejoin="round">
        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
      </svg>
    ),
  },
  {
    label: 'Распивы от 5 мл',
    icon: (
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" strokeLinejoin="round">
        <path d="M12 2C8 2 5 5 5 9c0 5 7 13 7 13s7-8 7-13c0-4-3-7-7-7z" />
        <circle cx="12" cy="9" r="2" />
      </svg>
    ),
  },
  {
    label: 'Доставка по России',
    icon: (
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" strokeLinejoin="round">
        <rect x="1" y="3" width="15" height="13" rx="1" />
        <path d="M16 8h4l3 3v5h-7V8z" />
        <circle cx="5.5" cy="18.5" r="2.5" />
        <circle cx="18.5" cy="18.5" r="2.5" />
      </svg>
    ),
  },
  {
    label: 'Заказ через Telegram',
    icon: (
      <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor">
        <path d="M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm5.894 8.221l-1.97 9.28c-.145.658-.537.818-1.084.508l-3-2.21-1.447 1.394c-.16.16-.295.295-.605.295l.213-3.053 5.56-5.023c.242-.213-.054-.333-.373-.12l-6.871 4.326-2.962-.924c-.643-.204-.657-.643.136-.953l11.57-4.461c.537-.194 1.006.131.833.941z"/>
      </svg>
    ),
  },
];

export default function TrustStrip() {
  return (
    <div className="bg-cream-100 border-t border-cream-200">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 py-4 flex flex-wrap items-center justify-center gap-x-8 gap-y-2">
        {TRUST_ITEMS.map((item) => (
          <span key={item.label} className="flex items-center gap-2 text-xs text-ink-500 tracking-wide">
            <span className="text-gold-500">{item.icon}</span>
            {item.label}
          </span>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Проверить сборку**

```bash
npm run build
```

Ожидаемый результат: `✓ Compiled successfully`, без ошибок TypeScript.

- [ ] **Step 3: Commit**

```bash
git add src/components/TrustStrip.tsx
git commit -m "feat: SVG icons in TrustStrip instead of dots"
```

---

### Task 6: Финальная проверка

- [ ] **Step 1: Полная сборка**

```bash
npm run build
```

Ожидаемый результат: все страницы генерируются, 0 TypeScript ошибок, 0 ESLint ошибок.

- [ ] **Step 2: Визуальная проверка в dev**

```bash
npm run dev
```

Открыть `http://localhost:3000` и проверить:
- Главная: карточки поднимаются при hover, бренд меняет цвет
- Главная: цифры 01-04 появляются с stagger при скролле до секции
- Главная: TrustStrip показывает иконки вместо точек
- Страница товара: стрелки галереи округлые, dot-индикаторы видны снизу фото
- Переход между страницами: плавный fade
