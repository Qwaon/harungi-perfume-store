# Order CRM Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Превратить заказ из «сообщения в чат» в запись со статусом в Airtable, управляемую inline-кнопками в Telegram, и показать клиенту номер заказа.

**Architecture:** Один Cloudflare Worker с двумя маршрутами: `POST /` (заказ с сайта → создать запись в Airtable `Orders` + отправить в чат сообщение с inline-кнопками) и `POST /tg` (callback от Telegram → patch статуса в Airtable + редактирование сообщения). Airtable — источник правды по статусу; деградация без потери лида. Сайт читает `orderNumber` из ответа и показывает его на экране успеха.

**Tech Stack:** Cloudflare Workers (vanilla JS, `fetch` handler), Airtable REST API, Telegram Bot API (`sendMessage`/`editMessageText`/`answerCallbackQuery`/`setWebhook`), Next.js 14 / TypeScript / React (модалки), Framer Motion.

> **Тестов в проекте нет.** Проверка корректности — `npm run build` (TypeScript) для фронта и ручной прогон для Worker'а. План использует `build` + ручные проверки вместо unit-тестов, как принято в этом репозитории (см. CLAUDE.md).

---

## File Structure

- **Modify** `workers/order-webhook.js` — добавить роутинг по `URL.pathname`, Airtable create/patch, inline-кнопки, обработку `POST /tg`, расширенный формат сообщения. Файл небольшой (95 строк) — расширяем в нём же, без разбиения.
- **Modify** `src/types/index.ts` — добавить поле `type` в `OrderPayload`/`CartOrderPayload`, добавить `tgUserId?`, добавить тип ответа `OrderResponse`.
- **Modify** `src/components/OrderModal.tsx` — `type` в payload, чтение `orderNumber`, экран успеха с номером + «что дальше» + кнопка «Написать менеджеру».
- **Modify** `src/components/CartCheckoutModal.tsx` — то же для корзины.
- **Modify** `CLAUDE.md` — раздел «Корзина и заявки»: новая таблица Airtable `Orders`, env Worker, `setWebhook`.
- **Manual (вне кода)** Airtable: создать таблицу `Orders`; Worker env vars; `setWebhook`. Шаги задокументированы в Task 7.

---

## Task 1: Типы — `type`, `tgUserId`, `OrderResponse`

**Files:**
- Modify: `src/types/index.ts:56-82`

- [ ] **Step 1: Добавить `type` и `tgUserId` в `OrderPayload`, `type` в `CartOrderPayload`, и новый `OrderResponse`**

Замени блок `OrderPayload` + `CartOrderPayload` (строки 56-82) на:

```typescript
export interface OrderPayload {
  name: string;
  contact: string;
  perfumeId: string;
  perfumeName: string;
  brand: string;
  volume: Volume;
  volumeLabel: string;
  price: number;
  source: string;
  pageUrl: string;
  pagePath: string;
  timestamp: string;
  messageType: 'order' | 'consultation';
  // 'single' для обычного заказа, 'consultation' для консультации.
  type: 'single' | 'consultation';
  // Telegram user id (только при заказе из Mini App), для будущей привязки.
  tgUserId?: string;
}

export interface CartOrderPayload {
  name: string;
  contact: string;
  items: CartItem[];
  total: number;
  source: string;
  pageUrl: string;
  pagePath: string;
  timestamp: string;
  messageType: 'cart-order';
  type: 'cart';
  tgUserId?: string;
}

// Ответ Worker'а на POST / — orderNumber может быть null, если Airtable недоступен.
export interface OrderResponse {
  ok: boolean;
  orderNumber?: number | null;
  error?: string;
}
```

- [ ] **Step 2: Проверить сборку TypeScript**

Run: `npm run build`
Expected: FAIL — TypeScript ругается, что в `OrderModal.tsx` и `CartCheckoutModal.tsx` payload не содержит обязательного поля `type`. Это ожидаемо; чиним в Task 2 и Task 3.

- [ ] **Step 3: Commit**

```bash
git add src/types/index.ts
git commit -m "feat(types): add order type field and OrderResponse"
```

---

## Task 2: OrderModal — `type` в payload + чтение `orderNumber`

**Files:**
- Modify: `src/components/OrderModal.tsx:35-70` (sendOrder, состояние), `:142-178` (payload + handleSubmit), `:252-268` (экран успеха)

- [ ] **Step 1: Добавить состояние номера заказа**

В `OrderModal` после строки `const [openedAt, setOpenedAt] = useState<number | null>(null);` (строка 76) добавь:

```typescript
  const [orderNumber, setOrderNumber] = useState<number | null>(null);
```

- [ ] **Step 2: Изменить `sendOrder`, чтобы возвращать распарсенный ответ**

Замени функцию `sendOrder` (строки 54-68) на:

```typescript
async function sendOrder(payload: OrderPayload): Promise<{ ok: boolean; orderNumber: number | null }> {
  if (!ORDER_WEBHOOK_URL) {
    return { ok: false, orderNumber: null };
  }

  const res = await fetch(ORDER_WEBHOOK_URL, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(payload),
  });

  if (!res.ok) {
    return { ok: false, orderNumber: null };
  }

  const data = await res.json().catch(() => ({}));
  return { ok: true, orderNumber: typeof data.orderNumber === 'number' ? data.orderNumber : null };
}
```

- [ ] **Step 3: Добавить `type` (и `tgUserId`) в payload**

В `handleSubmit` в объекте `payload` (строки 142-156), после `messageType: 'order',` добавь:

```typescript
      type: 'single',
      tgUserId: user?.id != null ? String(user.id) : undefined,
```

- [ ] **Step 4: Использовать новый возврат `sendOrder` и сохранять номер**

Замени строку `const sent = await sendOrder(payload);` (строка 162) и следующий `if (sent) {` блок до конца `else`-ветки (строки 162-178) на:

```typescript
      const result = await sendOrder(payload);

      if (result.ok) {
        window.localStorage.setItem(LAST_SUBMIT_KEY, String(Date.now()));
        trackEvent('order_submit', { perfumeId, volume, price });
        if (isTelegram) {
          appendOrder({ items: [[perfumeId, volume, 1, price]], total: price, type: 'order' });
        }
        setOrderNumber(result.orderNumber);
        setStatus('success');
      } else {
        trackEvent('order_fallback', { perfumeId, volume });
        if (isTelegram) {
          appendOrder({ items: [[perfumeId, volume, 1, price]], total: price, type: 'order' });
        }
        openFallback(payload);
        setOrderNumber(null);
        setStatus('success');
      }
```

- [ ] **Step 5: Сбрасывать номер в `handleClose`**

В `handleClose` внутри `setTimeout` (строки 188-195), после `setErrorMsg('');` добавь:

```typescript
      setOrderNumber(null);
```

- [ ] **Step 6: Обновить экран успеха**

Замени блок экрана успеха — от `<h3 className="font-display text-3xl font-light text-ink-900 mb-3">Заявка отправлена</h3>` и его `<p>` (строки 263-266) на:

```tsx
                      <h3 className="font-display text-3xl font-light text-ink-900 mb-3">
                        {orderNumber != null ? `Заявка №${orderNumber} принята` : 'Заявка отправлена'}
                      </h3>
                      <div className="text-ink-500 text-sm leading-relaxed mb-8 text-left bg-cream-100 rounded-xl px-4 py-3" style={{ boxShadow: '0px 0px 0px 1px #e8e6dc' }}>
                        <p className="mb-2">Что дальше:</p>
                        <p className="mb-1">1. Менеджер свяжется в Telegram или по телефону для подтверждения.</p>
                        <p>2. Оплата — после подтверждения (перевод или при встрече).</p>
                      </div>
                      <a
                        href={TELEGRAM_DIRECT_URL}
                        target="_blank"
                        rel="noopener noreferrer"
                        className="btn-outline w-full mb-3 inline-flex items-center justify-center"
                      >
                        Написать менеджеру
                      </a>
```

- [ ] **Step 7: Проверить сборку**

Run: `npm run build`
Expected: `OrderModal.tsx` больше не вызывает ошибок типов. (Сборка ещё может падать на `CartCheckoutModal.tsx` — чиним в Task 3.)

- [ ] **Step 8: Commit**

```bash
git add src/components/OrderModal.tsx
git commit -m "feat(order): send type, show order number and next-steps on success"
```

---

## Task 3: CartCheckoutModal — `type` в payload + чтение `orderNumber`

**Files:**
- Modify: `src/components/CartCheckoutModal.tsx:60-198` (state, handleSubmit), `:246-266` (экран успеха)

- [ ] **Step 1: Добавить состояние номера заказа**

После `const [openedAt, setOpenedAt] = useState<number | null>(null);` (строка 72) добавь:

```typescript
  const [orderNumber, setOrderNumber] = useState<number | null>(null);
```

- [ ] **Step 2: Добавить `type` и `tgUserId` в payload**

В `handleSubmit` в объекте `payload` (строки 142-152), после `messageType: 'cart-order',` добавь:

```typescript
      type: 'cart',
      tgUserId: user?.id != null ? String(user.id) : undefined,
```

- [ ] **Step 3: Читать `orderNumber` из ответа webhook**

Замени блок отправки и веток `if (sent)` / `else` (строки 157-193) на:

```typescript
    try {
      let sent = false;
      let receivedOrderNumber: number | null = null;
      if (ORDER_WEBHOOK_URL) {
        const res = await fetch(ORDER_WEBHOOK_URL, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload),
        });
        sent = res.ok;
        if (res.ok) {
          const data = await res.json().catch(() => ({}));
          receivedOrderNumber = typeof data.orderNumber === 'number' ? data.orderNumber : null;
        }
      }

      if (sent) {
        window.localStorage.setItem(LAST_SUBMIT_KEY, String(Date.now()));
        trackEvent('order_submit', { itemCount: items.length, total });
        if (isTelegram) {
          appendOrder({
            items: items.map((i) => [i.perfumeId, i.volume, i.quantity, i.price]),
            total,
            type: 'cart-order',
          });
        }
        setOrderNumber(receivedOrderNumber);
        setStatus('success');
        onSuccess();
      } else {
        const fallbackText = buildFallbackText(items, total, trimmedName, trimmedContact);
        window.open(`${TELEGRAM_URL}?text=${encodeURIComponent(fallbackText)}`, '_blank');
        trackEvent('order_fallback', { itemCount: items.length });
        if (isTelegram) {
          appendOrder({
            items: items.map((i) => [i.perfumeId, i.volume, i.quantity, i.price]),
            total,
            type: 'cart-order',
          });
        }
        setOrderNumber(null);
        setStatus('success');
        onSuccess();
      }
```

- [ ] **Step 4: Сбрасывать номер в `handleClose`**

В `handleClose` внутри `setTimeout` (строки 105-111), после `setErrorMsg('');` добавь:

```typescript
      setOrderNumber(null);
```

- [ ] **Step 5: Обновить экран успеха**

Замени заголовок и `<p>` экрана успеха (строки 257-262) — от `<h3 className="font-display text-3xl font-light text-ink-900 mb-3">` до закрывающего `</p>` подсказки — на:

```tsx
                      <h3 className="font-display text-3xl font-light text-ink-900 mb-3">
                        {orderNumber != null ? `Заявка №${orderNumber} принята` : 'Заявка отправлена'}
                      </h3>
                      <div className="text-ink-500 text-sm leading-relaxed mb-8 text-left bg-cream-100 rounded-xl px-4 py-3" style={{ boxShadow: '0px 0px 0px 1px #e8e6dc' }}>
                        <p className="mb-2">Что дальше:</p>
                        <p className="mb-1">1. Менеджер свяжется в Telegram или по телефону для подтверждения.</p>
                        <p>2. Оплата — после подтверждения (перевод или при встрече).</p>
                      </div>
                      <a
                        href={TELEGRAM_URL}
                        target="_blank"
                        rel="noopener noreferrer"
                        className="btn-outline w-full mb-3 inline-flex items-center justify-center"
                      >
                        Написать менеджеру
                      </a>
```

- [ ] **Step 6: Проверить сборку**

Run: `npm run build`
Expected: PASS — TypeScript проходит, оба компонента шлют `type` и читают `orderNumber`.

- [ ] **Step 7: Commit**

```bash
git add src/components/CartCheckoutModal.tsx
git commit -m "feat(cart): send type, show order number and next-steps on success"
```

---

## Task 4: Worker — роутинг + расширенный формат сообщения

**Files:**
- Modify: `workers/order-webhook.js:1-46` (fetch handler → роутинг), `:62-76` (formatTelegramMessage)

- [ ] **Step 1: Вынести текущую логику в `handleOrder` и добавить роутинг по pathname**

Замени `export default { async fetch(request, env) { ... } };` (строки 1-46) на:

```javascript
export default {
  async fetch(request, env) {
    const allowedOrigin = env.ALLOWED_ORIGIN || '*';
    const url = new URL(request.url);

    if (request.method === 'OPTIONS') {
      return new Response(null, { status: 204, headers: corsHeaders(allowedOrigin) });
    }

    // Telegram callback (смена статуса по кнопке). Заполняется в Task 6.
    if (url.pathname === '/tg' && request.method === 'POST') {
      return handleTelegramCallback(request, env);
    }

    if (request.method !== 'POST') {
      return json({ ok: false, error: 'Method not allowed' }, 405, allowedOrigin);
    }

    return handleOrder(request, env, allowedOrigin);
  },
};

async function handleOrder(request, env, allowedOrigin) {
  try {
    const payload = await request.json();
    const validationError = validatePayload(payload);

    if (validationError) {
      return json({ ok: false, error: validationError }, 400, allowedOrigin);
    }

    // Создаём запись в Airtable (источник правды по статусу). Заполняется в Task 5.
    const order = await createAirtableOrder(payload, env);

    const text = formatTelegramMessage(payload, order && order.orderNumber);
    const sendBody = {
      chat_id: env.TELEGRAM_CHAT_ID,
      text,
    };
    // Кнопки статуса добавляются только если запись создана (есть recordId).
    if (order && order.recordId) {
      sendBody.reply_markup = statusKeyboard(order.recordId);
    }

    const telegramResponse = await fetch(`https://api.telegram.org/bot${env.TELEGRAM_BOT_TOKEN}/sendMessage`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(sendBody),
    });

    if (!telegramResponse.ok) {
      const errorText = await telegramResponse.text();
      return json({ ok: false, error: errorText }, 502, allowedOrigin);
    }

    return json({ ok: true, orderNumber: order ? order.orderNumber : null }, 200, allowedOrigin);
  } catch (error) {
    return json({ ok: false, error: error instanceof Error ? error.message : 'Unknown error' }, 500, allowedOrigin);
  }
}
```

> Примечание: `createAirtableOrder`, `statusKeyboard`, `handleTelegramCallback` определяются в Task 5 и Task 6. До этого Worker не задеплоится корректно, но локально это один файл — задачи идут подряд, коммитим после Task 6 как рабочий Worker. Сборки фронта это не касается.

- [ ] **Step 2: Расширить `formatTelegramMessage` (номер заказа + состав корзины)**

Замени `formatTelegramMessage` (строки 62-76) на:

```javascript
function formatTelegramMessage(payload, orderNumber) {
  const title = payload.messageType === 'consultation' ? 'Новая консультация' : 'Новый заказ';
  const header = orderNumber ? `${title} №${orderNumber}` : title;
  const lines = [header, ''];

  if (Array.isArray(payload.items)) {
    // Заказ-корзина.
    payload.items.forEach((item) => {
      const qtyPart = item.quantity > 1 ? ` ×${item.quantity}` : '';
      const linePrice = Number(item.price * item.quantity).toLocaleString('ru-RU');
      lines.push(`• ${item.brand} — ${item.perfumeName} ${item.volumeLabel}${qtyPart} — ${linePrice} ₽`);
    });
    lines.push(`Итого: ${Number(payload.total).toLocaleString('ru-RU')} ₽`);
  } else {
    // Одиночный заказ / консультация.
    lines.push(`Аромат: ${payload.brand} — ${payload.perfumeName}`);
    lines.push(`Объём: ${payload.volumeLabel || payload.volume}`);
    lines.push(`Цена: ${Number(payload.price).toLocaleString('ru-RU')} ₽`);
  }

  lines.push(
    '',
    `Имя: ${payload.name}`,
    `Контакт: ${payload.contact}`,
    `Источник: ${payload.source}`,
    `Страница: ${payload.pageUrl}`,
    `Время: ${payload.timestamp}`
  );
  return lines.join('\n');
}
```

- [ ] **Step 3: Обновить `validatePayload` под корзину и одиночный заказ**

Замени `validatePayload` (строки 48-60) на:

```javascript
function validatePayload(payload) {
  if (!payload || typeof payload !== 'object') return 'Invalid payload';

  const baseRequired = ['name', 'contact', 'source', 'pageUrl', 'timestamp'];
  for (const key of baseRequired) {
    if (!payload[key]) return `Missing field: ${key}`;
  }

  const isCart = Array.isArray(payload.items);
  if (isCart) {
    if (payload.items.length === 0) return 'Empty cart';
    if (payload.total == null) return 'Missing field: total';
  } else {
    for (const key of ['perfumeName', 'brand', 'volume', 'price']) {
      if (!payload[key]) return `Missing field: ${key}`;
    }
  }

  if (String(payload.name).trim().length < 2) return 'Name is too short';
  if (String(payload.contact).trim().length < 4) return 'Contact is too short';

  return null;
}
```

- [ ] **Step 4: Не коммитить пока — переходим к Task 5**

Worker сейчас ссылается на ещё не определённые `createAirtableOrder`, `statusKeyboard`, `handleTelegramCallback`. Коммит — после Task 6, когда Worker станет целостным.

---

## Task 5: Worker — создание записи в Airtable + клавиатура статусов

**Files:**
- Modify: `workers/order-webhook.js` (добавить функции в конец файла, перед `corsHeaders`)

- [ ] **Step 1: Добавить константы статусов и хелперы Airtable**

Вверху файла, перед `export default`, добавь:

```javascript
// Статусы заказа и их подписи для кнопок/сообщения.
const STATUS_LABELS = {
  new: 'Новый',
  accepted: '✅ Принят',
  shipped: '📦 Отправлен',
  done: '✔️ Выполнен',
  canceled: '✖️ Отмена',
};
```

- [ ] **Step 2: Добавить `createAirtableOrder` и `statusKeyboard`**

В конец файла (перед функцией `corsHeaders`) добавь:

```javascript
// Создаёт запись заказа в Airtable Orders. Возвращает { recordId, orderNumber }
// или null при любой ошибке/отсутствии ключей (деградация: заказ всё равно уйдёт в чат).
async function createAirtableOrder(payload, env) {
  if (!env.AIRTABLE_API_KEY || !env.AIRTABLE_BASE_ID) return null;
  const table = env.AIRTABLE_ORDERS_TABLE || 'Orders';

  const isCart = Array.isArray(payload.items);
  let itemsText;
  let total;
  let type;

  if (isCart) {
    itemsText = payload.items
      .map((i) => {
        const qty = i.quantity > 1 ? ` ×${i.quantity}` : '';
        return `${i.brand} — ${i.perfumeName} ${i.volumeLabel}${qty} — ${Number(i.price * i.quantity)} ₽`;
      })
      .join('\n');
    total = Number(payload.total);
    type = 'cart';
  } else {
    itemsText = `${payload.brand} — ${payload.perfumeName} ${payload.volumeLabel || payload.volume} — ${Number(payload.price)} ₽`;
    total = Number(payload.price);
    type = payload.type || (payload.messageType === 'consultation' ? 'consultation' : 'single');
  }

  const fields = {
    status: 'new',
    customerName: String(payload.name),
    contact: String(payload.contact),
    items: itemsText,
    total,
    type,
    source: String(payload.source || ''),
    pageUrl: String(payload.pageUrl || ''),
    tgUserId: payload.tgUserId ? String(payload.tgUserId) : '',
  };

  try {
    const res = await fetch(
      `https://api.airtable.com/v0/${env.AIRTABLE_BASE_ID}/${encodeURIComponent(table)}`,
      {
        method: 'POST',
        headers: {
          Authorization: `Bearer ${env.AIRTABLE_API_KEY}`,
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ fields, typecast: true }),
      }
    );
    if (!res.ok) return null;
    const data = await res.json();
    return { recordId: data.id, orderNumber: data.fields && data.fields.orderNumber };
  } catch {
    return null;
  }
}

// Inline-клавиатура для смены статуса. callback_data = "s:<recordId>:<status>".
function statusKeyboard(recordId, activeStatus) {
  const btn = (status) => ({
    text: activeStatus === status ? `• ${STATUS_LABELS[status]}` : STATUS_LABELS[status],
    callback_data: `s:${recordId}:${status}`,
  });
  return {
    inline_keyboard: [
      [btn('accepted'), btn('shipped')],
      [btn('done'), btn('canceled')],
    ],
  };
}
```

- [ ] **Step 3: Проверить синтаксис файла**

Run: `node --check workers/order-webhook.js`
Expected: нет вывода (синтаксис валиден). `handleTelegramCallback` ещё не определён, но `--check` проверяет только синтаксис, не разрешение имён — пройдёт. Если ругается — исправь синтаксис.

- [ ] **Step 4: Не коммитить — переходим к Task 6 (Worker станет целостным)**

---

## Task 6: Worker — обработка callback от Telegram

**Files:**
- Modify: `workers/order-webhook.js` (добавить `handleTelegramCallback` + Airtable patch + edit message)

- [ ] **Step 1: Добавить `handleTelegramCallback`, `patchAirtableStatus`, `tg` хелпер**

В конец файла (перед `corsHeaders`) добавь:

```javascript
// Обработка нажатия inline-кнопки статуса. Привязывается через setWebhook на /tg.
async function handleTelegramCallback(request, env) {
  // Защита: только Telegram знает секрет (передаётся в заголовке при setWebhook).
  const secret = request.headers.get('X-Telegram-Bot-Api-Secret-Token');
  if (env.TELEGRAM_WEBHOOK_SECRET && secret !== env.TELEGRAM_WEBHOOK_SECRET) {
    return new Response('unauthorized', { status: 401 });
  }

  let update;
  try {
    update = await request.json();
  } catch {
    return new Response('ok'); // не валидный JSON — игнорируем
  }

  const cq = update && update.callback_query;
  if (!cq || !cq.data || !cq.data.startsWith('s:')) {
    return new Response('ok'); // не наш апдейт — игнорируем
  }

  // callback_data = "s:<recordId>:<status>"
  const parts = cq.data.split(':');
  const recordId = parts[1];
  const status = parts[2];
  const label = STATUS_LABELS[status];

  if (!recordId || !label) {
    await answerCallback(cq.id, 'Неизвестный статус', env);
    return new Response('ok');
  }

  const patched = await patchAirtableStatus(recordId, status, env);

  if (!patched) {
    await answerCallback(cq.id, 'Не удалось обновить, попробуйте ещё', env);
    return new Response('ok');
  }

  // Зеркалим статус в сообщение: дописываем строку и подсвечиваем активную кнопку.
  const msg = cq.message;
  if (msg) {
    const baseText = msg.text || '';
    // Убираем прошлую строку статуса, если была, чтобы не плодить дубли.
    const cleaned = baseText.replace(/\n\nСтатус: .*$/s, '');
    const newText = `${cleaned}\n\nСтатус: ${label}`;
    const finished = status === 'done' || status === 'canceled';
    await tg('editMessageText', {
      chat_id: msg.chat.id,
      message_id: msg.message_id,
      text: newText,
      reply_markup: finished ? { inline_keyboard: [] } : statusKeyboard(recordId, status),
    }, env);
  }

  await answerCallback(cq.id, `Статус: ${label}`, env);
  return new Response('ok');
}

// PATCH статуса в Airtable. true при успехе, false при ошибке/отсутствии ключей.
async function patchAirtableStatus(recordId, status, env) {
  if (!env.AIRTABLE_API_KEY || !env.AIRTABLE_BASE_ID) return false;
  const table = env.AIRTABLE_ORDERS_TABLE || 'Orders';
  try {
    const res = await fetch(
      `https://api.airtable.com/v0/${env.AIRTABLE_BASE_ID}/${encodeURIComponent(table)}/${recordId}`,
      {
        method: 'PATCH',
        headers: {
          Authorization: `Bearer ${env.AIRTABLE_API_KEY}`,
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ fields: { status }, typecast: true }),
      }
    );
    return res.ok;
  } catch {
    return false;
  }
}

// Тост менеджеру при нажатии кнопки.
async function answerCallback(callbackQueryId, text, env) {
  await tg('answerCallbackQuery', { callback_query_id: callbackQueryId, text }, env).catch(() => {});
}

// Тонкая обёртка над Telegram Bot API.
async function tg(method, body, env) {
  return fetch(`https://api.telegram.org/bot${env.TELEGRAM_BOT_TOKEN}/${method}`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(body),
  });
}
```

- [ ] **Step 2: Проверить синтаксис целого файла**

Run: `node --check workers/order-webhook.js`
Expected: нет вывода. Все функции (`createAirtableOrder`, `statusKeyboard`, `handleTelegramCallback`, `patchAirtableStatus`, `answerCallback`, `tg`, `formatTelegramMessage`, `validatePayload`, `handleOrder`) теперь определены.

- [ ] **Step 3: Прогнать сборку фронта (sanity — Worker фронт не ломает)**

Run: `npm run build`
Expected: PASS.

- [ ] **Step 4: Commit (Worker целостен)**

```bash
git add workers/order-webhook.js
git commit -m "feat(worker): persist orders to Airtable + Telegram status buttons"
```

---

## Task 7: Документация и ручная настройка

**Files:**
- Modify: `CLAUDE.md` (раздел «Корзина и заявки» + переменные окружения)

- [ ] **Step 1: Обновить раздел «Корзина и заявки» в CLAUDE.md**

Найди в `CLAUDE.md` абзац, начинающийся с «Webhook схема: сайт POST на `NEXT_PUBLIC_ORDER_WEBHOOK_URL`...» и сразу после него добавь:

```markdown

**Учёт заказов и статусы (Order CRM).** Worker, помимо отправки в чат, создаёт запись в таблице Airtable `Orders` (`status=new`) и шлёт сообщение с inline-кнопками статуса (`✅ Принят` / `📦 Отправлен` / `✔️ Выполнен` / `✖️ Отмена`). Нажатие кнопки бьёт во второй маршрут Worker'а `POST /tg` (`callback_query`), который патчит `status` в Airtable и редактирует сообщение. Airtable — источник правды по статусу; чат — зеркало. Если Airtable недоступен — заказ всё равно уходит в чат (без кнопок), сайт показывает успех без номера (лид не теряется). `callback_data` формата `s:<recordId>:<status>`. Маршрут `/tg` защищён заголовком `X-Telegram-Bot-Api-Secret-Token` (= `TELEGRAM_WEBHOOK_SECRET`).

Таблица `Orders` (Airtable): `orderNumber` (Autonumber), `status` (Single select: new/accepted/shipped/done/canceled), `customerName`, `contact`, `items` (Long text), `total` (Number), `type` (Single select: single/cart/consultation), `source`, `pageUrl` (URL), `tgUserId`, `createdAt` (Created time), `note` (Long text, ручные заметки менеджера).

Env Worker'а (Cloudflare → Settings → Variables): существующие `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `ALLOWED_ORIGIN` + новые `AIRTABLE_API_KEY`, `AIRTABLE_BASE_ID`, `AIRTABLE_ORDERS_TABLE` (=`Orders`), `TELEGRAM_WEBHOOK_SECRET`. Привязка бота к callback-маршруту (разово):
`curl "https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://<worker-host>/tg&secret_token=<TELEGRAM_WEBHOOK_SECRET>&allowed_updates=[\"callback_query\"]"`
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: document order CRM (Airtable Orders, /tg webhook, env)"
```

- [ ] **Step 3: Ручная настройка (вне репозитория — выполняет владелец)**

Эти шаги не в коде; выполнить в Airtable/Cloudflare/BotFather:

1. **Airtable:** в той же базе, что `Catalog`, создать таблицу `Orders` с полями из Step 1 выше. Ровно эти имена полей (camelCase) — Worker шлёт их as-is с `typecast:true`.
2. **Cloudflare Worker env:** добавить `AIRTABLE_API_KEY`, `AIRTABLE_BASE_ID`, `AIRTABLE_ORDERS_TABLE=Orders`, `TELEGRAM_WEBHOOK_SECRET` (сгенерировать случайную строку), задеплоить Worker.
3. **setWebhook:** выполнить curl из Step 1 с реальными значениями.
4. **Проверка:** `curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"` — `url` указывает на `/tg`, ошибок нет.

---

## Task 8: Ручная приёмка (end-to-end)

**Files:** нет (проверка)

- [ ] **Step 1: Одиночный заказ**

На локали/проде оформить заказ одного аромата (`OrderModal`). Ожидаемо:
- В Airtable `Orders` появилась запись `status=new`, поля заполнены.
- В Telegram-чат пришло сообщение «Новый заказ №N» с четырьмя кнопками.
- На сайте экран успеха показывает «Заявка №N принята» + шаги + кнопку «Написать менеджеру».

- [ ] **Step 2: Заказ-корзина**

Оформить заказ из 2+ позиций (`CartCheckoutModal`). Ожидаемо: сообщение содержит список позиций + «Итого»; запись в Airtable с `items` многострочным и `type=cart`.

- [ ] **Step 3: Смена статуса кнопкой**

В чате нажать `✅ Принят`. Ожидаемо: всплывает тост «Статус: ✅ Принят»; в сообщение дописана строка «Статус: ✅ Принят»; активная кнопка помечена `•`; в Airtable `status=accepted`. Нажать `✔️ Выполнен` → кнопки исчезают, статус в Airtable `done`.

- [ ] **Step 4: Деградация Airtable**

Временно убрать `AIRTABLE_API_KEY` из env Worker'а (или задать неверный), оформить заказ. Ожидаемо: сообщение в чат пришло **без** кнопок; на сайте экран успеха **без** номера; лид не потерян. Вернуть ключ обратно.

- [ ] **Step 5: Финальный коммит при необходимости**

Если на приёмке нашлись правки — внести и закоммитить отдельными коммитами. Иначе задача завершена.
